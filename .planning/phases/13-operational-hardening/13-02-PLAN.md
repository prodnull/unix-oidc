---
phase: 13-operational-hardening
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - unix-oidc-agent/Cargo.toml
  - unix-oidc-agent/src/main.rs
  - unix-oidc-agent/src/daemon/socket.rs
  - contrib/systemd/unix-oidc-agent.service
  - contrib/systemd/unix-oidc-agent.socket
autonomous: true
requirements: [OPS-01, OPS-02, OPS-04]

must_haves:
  truths:
    - "systemd socket unit creates a user socket at $XDG_RUNTIME_DIR/unix-oidc-agent.sock with mode 0600"
    - "systemd service unit has all hardening directives from CONTEXT.md decisions"
    - "Agent detects LISTEN_FDS and uses the inherited socket instead of creating its own"
    - "Agent sends sd-notify READY=1 after socket bind + config validation + JWKS fetch attempt"
    - "Agent handles SIGTERM/SIGINT gracefully: stops accepting, drains 5s, cleans up credentials"
  artifacts:
    - path: "contrib/systemd/unix-oidc-agent.service"
      provides: "systemd user service unit with Type=notify and hardening"
      contains: "NoNewPrivileges=yes"
    - path: "contrib/systemd/unix-oidc-agent.socket"
      provides: "systemd socket activation unit"
      contains: "ListenStream=%t/unix-oidc-agent.sock"
    - path: "unix-oidc-agent/src/daemon/socket.rs"
      provides: "acquire_listener() with systemd socket activation fallback"
      contains: "ListenFd"
  key_links:
    - from: "unix-oidc-agent/src/daemon/socket.rs"
      to: "listenfd"
      via: "ListenFd::from_env() for socket activation detection"
      pattern: "ListenFd::from_env"
    - from: "unix-oidc-agent/src/main.rs"
      to: "sd_notify"
      via: "sd_notify::notify(false, &[NotifyState::Ready])"
      pattern: "NotifyState::Ready"
    - from: "unix-oidc-agent/src/daemon/socket.rs"
      to: "tokio::signal"
      via: "signal(SignalKind::terminate()) in select! loop"
      pattern: "SignalKind::terminate"
---

<objective>
Add systemd user service integration: socket activation with standalone fallback, sd-notify readiness signaling, graceful SIGTERM/SIGINT shutdown, and hardened systemd unit files.

Purpose: The agent daemon can be managed via `systemctl --user enable --now unix-oidc-agent` on production Linux systems with proper lifecycle management and security hardening.
Output: systemd service + socket units in contrib/, socket acquisition and signal handling in daemon code.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-operational-hardening/13-CONTEXT.md
@.planning/phases/13-operational-hardening/13-RESEARCH.md
@.planning/phases/13-operational-hardening/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — TimeoutsConfig available in AgentConfig -->
From unix-oidc-agent/src/config.rs (after Plan 01):
```rust
pub struct AgentConfig {
    pub issuer: String,
    pub client_id: String,
    pub socket_path: Option<PathBuf>,
    pub crypto: CryptoConfig,
    pub timeouts: TimeoutsConfig,
}
pub struct TimeoutsConfig {
    pub jwks_http_timeout_secs: u64,
    pub device_flow_http_timeout_secs: u64,
    pub clock_skew_future_secs: u64,
    pub clock_skew_staleness_secs: u64,
    pub jwks_cache_ttl_secs: u64,
    pub ipc_idle_timeout_secs: u64,
}
```

From unix-oidc-agent/src/daemon/socket.rs:
```rust
pub struct AgentServer {
    pub socket_path: PathBuf,
    state: Arc<RwLock<AgentState>>,
}
impl AgentServer {
    pub async fn serve(&self) -> Result<(), std::io::Error>;
}
```

From unix-oidc-agent/src/main.rs:
```rust
async fn run_serve(socket: Option<String>) -> anyhow::Result<()>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create systemd unit files</name>
  <files>contrib/systemd/unix-oidc-agent.service, contrib/systemd/unix-oidc-agent.socket</files>
  <action>
    1. Create contrib/systemd/ directory.
    2. Create unix-oidc-agent.socket:
       - [Unit] Description=OIDC Authentication Agent Socket, Documentation=https://github.com/prodnull/unix-oidc
       - [Socket] ListenStream=%t/unix-oidc-agent.sock, SocketMode=0600
       - [Install] WantedBy=sockets.target
    3. Create unix-oidc-agent.service per RESEARCH.md:
       - [Unit] Description=OIDC Authentication Agent, Requires=unix-oidc-agent.socket, After=unix-oidc-agent.socket network-online.target
       - [Service] Type=notify, ExecStart=%h/.cargo/bin/unix-oidc-agent serve, Restart=on-failure, RestartSec=5s
       - All hardening directives from CONTEXT.md: NoNewPrivileges=yes, ProtectSystem=strict, ProtectHome=read-only, MemoryDenyWriteExecute=yes, RestrictNamespaces=yes, RestrictRealtime=yes, RestrictSUIDSGID=yes, PrivateTmp=yes, SystemCallFilter=@system-service @network-io
       - ReadWritePaths=%h/.local/share/unix-oidc %h/.config/unix-oidc (required for file storage backend under ProtectSystem=strict)
       - Add comment: "# If using YubiKey/PKCS#11 signer, uncomment: MemoryDenyWriteExecute=no"
       - [Install] WantedBy=default.target
  </action>
  <verify>
    <automated>test -f /Users/cbc/code/apps/unix-oidc/contrib/systemd/unix-oidc-agent.service && test -f /Users/cbc/code/apps/unix-oidc/contrib/systemd/unix-oidc-agent.socket && echo "OK"</automated>
  </verify>
  <done>Both unit files exist with correct structure, hardening directives, and socket activation wiring</done>
</task>

<task type="auto">
  <name>Task 2: Implement socket acquisition, sd-notify, signal handling, and graceful shutdown</name>
  <files>unix-oidc-agent/Cargo.toml, unix-oidc-agent/src/daemon/socket.rs, unix-oidc-agent/src/main.rs</files>
  <action>
    1. Add dependencies to Cargo.toml: `sd-notify = "0.5"`, `listenfd = "1.0"`.
    2. In socket.rs, add `pub fn acquire_listener(socket_path: &Path) -> std::io::Result<UnixListener>`:
       - Step 1: Check ListenFd::from_env() for systemd socket activation fd. If present, set_nonblocking(true) and convert to tokio UnixListener. Log "Socket acquired via systemd activation".
       - Step 2 (cfg(target_os = "macos")): Placeholder for launchd socket (implemented in Plan 04).
       - Step 3: Standalone fallback — remove existing socket file, UnixListener::bind(), set permissions 0600. Log "Socket bound in standalone mode".
    3. Refactor AgentServer::serve() to accept a pre-bound listener:
       - Change serve() signature to `pub async fn serve_with_listener(&self, listener: UnixListener)` or have serve() call acquire_listener() internally.
       - Replace the current `UnixListener::bind()` in serve() with `acquire_listener()`.
    4. Add graceful shutdown to serve loop per RESEARCH.md Pattern 5:
       - Register SIGTERM and SIGINT handlers BEFORE the select! loop (critical: register early so signals during startup are caught).
       - tokio::select! races listener.accept() against sigterm.recv() and sigint.recv().
       - On signal: log "SIGTERM/SIGINT — shutting down", break loop.
       - After loop: send sd_notify::notify(false, &[NotifyState::Stopping]).
       - Wait 5s drain period (tokio::time::sleep).
       - Run credential cleanup: zeroize keys best-effort, revoke tokens best-effort. Reuse existing cleanup patterns.
       - Replace println!("Press Ctrl+C to stop") in main.rs with structured tracing log.
    5. In run_serve() (main.rs), add sd-notify readiness sequence per RESEARCH.md Pattern 2:
       - Gate 1: socket acquired (acquire_listener or serve internal).
       - Gate 2: config validated (AgentConfig::load() succeeded — already happens).
       - Gate 3: JWKS prefetch attempt (best-effort; wrap in catch, WARN on failure, do not block).
       - Send READY=1: `sd_notify::notify(false, &[NotifyState::Ready, NotifyState::Status("unix-oidc-agent ready")])`.
       - Note: sd-notify is no-op when NOTIFY_SOCKET is not set (standalone mode) — safe to call unconditionally.
    6. Remove the current Shutdown IPC command calling std::process::exit(0) — replace with sending SIGTERM to self (or just remove it and rely on signal-based shutdown). If keeping the IPC command for convenience, have it raise SIGTERM: `unsafe { libc::kill(libc::getpid(), libc::SIGTERM) }`.
    7. Add a basic test: acquire_listener in standalone mode binds successfully and the socket file exists.
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p unix-oidc-agent 2>&1 | tail -30</automated>
  </verify>
  <done>Socket activation works via listenfd when LISTEN_FDS is set; standalone fallback binds its own socket; SIGTERM/SIGINT trigger graceful shutdown with 5s drain; sd-notify READY=1 sent after readiness gates; systemctl --user can manage the daemon lifecycle</done>
</task>

</tasks>

<verification>
- `cargo build -p unix-oidc-agent` — compiles with sd-notify and listenfd
- `cargo test -p unix-oidc-agent` — all tests pass including standalone socket test
- contrib/systemd/ files exist with correct content
- No `println!` or `std::process::exit(0)` in serve path
</verification>

<success_criteria>
- systemd service unit has Type=notify with all hardening directives
- systemd socket unit creates socket at %t/unix-oidc-agent.sock with 0600
- acquire_listener() detects LISTEN_FDS for socket activation, falls back to standalone bind
- sd-notify READY=1 sent after socket + config + JWKS gates
- SIGTERM/SIGINT handled gracefully with 5s drain and credential cleanup
- Existing serve() behavior preserved in standalone mode
</success_criteria>

<output>
After completion, create `.planning/phases/13-operational-hardening/13-02-SUMMARY.md`
</output>
