---
phase: 13-operational-hardening
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - contrib/launchd/com.unix-oidc.agent.plist.template
  - unix-oidc-agent/src/main.rs
  - unix-oidc-agent/src/daemon/socket.rs
autonomous: true
requirements: [OPS-03]

must_haves:
  truths:
    - "A launchd plist template exists with KeepAlive, RunAtLoad, and Sockets configuration"
    - "unix-oidc-agent install generates a plist with the correct binary path and socket path substituted"
    - "unix-oidc-agent uninstall removes the plist and unloads the agent"
    - "On macOS, the agent can accept connections via a launchd-activated socket"
  artifacts:
    - path: "contrib/launchd/com.unix-oidc.agent.plist.template"
      provides: "launchd plist template for macOS agent"
      contains: "KeepAlive"
    - path: "unix-oidc-agent/src/main.rs"
      provides: "Install and Uninstall subcommands"
      contains: "Commands::Install"
  key_links:
    - from: "unix-oidc-agent/src/main.rs"
      to: "contrib/launchd/com.unix-oidc.agent.plist.template"
      via: "install subcommand writes plist to ~/Library/LaunchAgents/"
      pattern: "LaunchAgents"
    - from: "unix-oidc-agent/src/daemon/socket.rs"
      to: "launch_activate_socket"
      via: "cfg(target_os = macos) launchd socket detection in acquire_listener"
      pattern: "launch_activate_socket"
---

<objective>
Add macOS launchd integration: plist template, install/uninstall subcommands, and launchd socket activation detection in acquire_listener().

Purpose: macOS users can start the agent automatically at login via `unix-oidc-agent install` without manual plist authoring.
Output: launchd plist template, Install/Uninstall CLI subcommands, macOS socket activation in acquire_listener.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-operational-hardening/13-CONTEXT.md
@.planning/phases/13-operational-hardening/13-RESEARCH.md
@.planning/phases/13-operational-hardening/13-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — acquire_listener exists with systemd + standalone paths -->
From unix-oidc-agent/src/daemon/socket.rs (after Plan 02):
```rust
pub fn acquire_listener(socket_path: &Path) -> std::io::Result<UnixListener> {
    // 1. systemd socket activation (listenfd)
    // 2. Placeholder for launchd (cfg target_os = macos)
    // 3. Standalone fallback
}
```

From unix-oidc-agent/src/main.rs (after Plan 02):
```rust
enum Commands {
    Login { ... },
    Provision { ... },
    Serve { ... },
    Status,
    Refresh,
    GetProof { ... },
    // Install and Uninstall to be added
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create launchd plist template and install/uninstall subcommands</name>
  <files>contrib/launchd/com.unix-oidc.agent.plist.template, unix-oidc-agent/src/main.rs</files>
  <action>
    1. Create contrib/launchd/ directory.
    2. Create com.unix-oidc.agent.plist.template — XML plist with:
       - Label: com.unix-oidc.agent
       - ProgramArguments: [{{BINARY_PATH}}, serve]
       - KeepAlive: true
       - RunAtLoad: true
       - Sockets dict with "Listeners" key:
         - SockFamily: Unix
         - SockPathName: {{SOCKET_PATH}}
         - SockPathMode: 384 (octal 0600)
       - StandardOutPath: {{HOME}}/Library/Logs/unix-oidc-agent.log
       - StandardErrorPath: {{HOME}}/Library/Logs/unix-oidc-agent.log
       - Use {{PLACEHOLDER}} syntax for values substituted at install time.
    3. Add Install and Uninstall subcommands to the CLI:
       ```
       Install {
           #[arg(long)]
           binary_path: Option<String>,  // Override; default: std::env::current_exe()
       },
       Uninstall,
       ```
    4. Implement `run_install(binary_path: Option<String>)`:
       - Determine binary path: --binary-path flag, or std::env::current_exe().
       - Determine socket path: $TMPDIR/unix-oidc-agent.sock (macOS sets TMPDIR per-user).
       - Read the template from compile-time include_str! or from a known path.
       - Substitute {{BINARY_PATH}}, {{SOCKET_PATH}}, {{HOME}} with real values.
       - Write to ~/Library/LaunchAgents/com.unix-oidc.agent.plist.
       - Run `launchctl load ~/Library/LaunchAgents/com.unix-oidc.agent.plist` via std::process::Command.
       - Print success message with instructions.
       - On Linux: print message directing user to systemd units in contrib/systemd/ instead.
    5. Implement `run_uninstall()`:
       - Run `launchctl unload ~/Library/LaunchAgents/com.unix-oidc.agent.plist`.
       - Remove the plist file.
       - Print success message.
       - On Linux: print message directing user to `systemctl --user disable --now unix-oidc-agent`.
    6. Add basic test: template substitution produces valid XML (check for no remaining {{}} placeholders).
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo build -p unix-oidc-agent && test -f contrib/launchd/com.unix-oidc.agent.plist.template && echo "OK"</automated>
  </verify>
  <done>Plist template exists; install subcommand substitutes placeholders and writes to ~/Library/LaunchAgents/; uninstall removes plist; Linux users get helpful redirect message</done>
</task>

<task type="auto">
  <name>Task 2: Add macOS launchd socket activation to acquire_listener</name>
  <files>unix-oidc-agent/src/daemon/socket.rs</files>
  <action>
    1. Add a cfg(target_os = "macos") module `launchd_socket` at the bottom of socket.rs (or in a separate file) per RESEARCH.md Pattern 10:
       - Declare extern "C" fn launch_activate_socket(name, fds, cnt) from libc FFI.
       - Implement `pub fn take(name: &str) -> Option<tokio::net::UnixListener>`:
         - Call launch_activate_socket with CString name.
         - If ret != 0 or cnt == 0: return None (not launched by launchd).
         - Extract first fd, free the fds pointer, wrap in std UnixListener via FromRawFd.
         - Set nonblocking, convert to tokio UnixListener.
    2. In acquire_listener(), fill in the macOS placeholder (step 2):
       - After the systemd check fails: try launchd_socket::take("Listeners").
       - If Some(listener): log "Socket acquired via launchd activation", return.
       - If None: fall through to standalone mode.
    3. The take() function is only compiled on macOS. On Linux, the step is skipped entirely via cfg.
    4. Testing: The FFI call requires actually being launched by launchd, so unit testing the full path is not feasible. Add a compile-test (cfg(target_os = "macos")) that verifies the module compiles. The integration test is manual: `launchctl load` + verify socket.
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo build -p unix-oidc-agent && echo "OK"</automated>
  </verify>
  <done>acquire_listener() checks launchd socket on macOS after systemd check; launchd_socket FFI module compiles on macOS; Linux build unaffected</done>
</task>

</tasks>

<verification>
- `cargo build -p unix-oidc-agent` — compiles on current platform
- contrib/launchd/com.unix-oidc.agent.plist.template exists with correct structure
- `cargo test -p unix-oidc-agent` — all tests pass
</verification>

<success_criteria>
- launchd plist template exists with KeepAlive, RunAtLoad, Sockets dict
- install subcommand substitutes {{BINARY_PATH}}, {{SOCKET_PATH}}, {{HOME}} and writes plist
- uninstall subcommand removes plist and runs launchctl unload
- acquire_listener() detects launchd socket on macOS via launch_activate_socket FFI
- Linux builds are unaffected by macOS-only code
</success_criteria>

<output>
After completion, create `.planning/phases/13-operational-hardening/13-04-SUMMARY.md`
</output>
