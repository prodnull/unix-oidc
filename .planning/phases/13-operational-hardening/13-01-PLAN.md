---
phase: 13-operational-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - unix-oidc-agent/Cargo.toml
  - unix-oidc-agent/src/config.rs
  - unix-oidc-agent/src/main.rs
  - pam-unix-oidc/src/audit.rs
  - pam-unix-oidc/src/oidc/jwks.rs
  - pam-unix-oidc/src/oidc/dpop.rs
  - pam-unix-oidc/src/oidc/validation.rs
autonomous: true
requirements: [OPS-07, OPS-08, OPS-09, OPS-10, OPS-12]

must_haves:
  truths:
    - "Operator can set jwks_cache_ttl_secs in config.yaml and the value is passed to JwksProvider::with_cache_ttl()"
    - "UNIX_OIDC_JWKS_CACHE_TTL env var overrides the config file value"
    - "Zero or negative timeout values cause config load to fail with a clear error"
    - "get_hostname() returns the POSIX gethostname(2) result, not an environment variable"
    - "jwks_http_timeout_secs replaces the hardcoded HTTP_TIMEOUT_SECS constant in jwks.rs"
    - "device_flow_http_timeout_secs replaces the hardcoded 30s timeout in the device flow and token refresh HTTP clients"
    - "clock_skew_future_secs replaces the hardcoded 5 in dpop.rs future-proof check"
    - "clock_skew_staleness_secs wires to DPoPConfig::max_proof_age and CLOCK_SKEW_TOLERANCE in validation.rs"
  artifacts:
    - path: "unix-oidc-agent/src/config.rs"
      provides: "TimeoutsConfig struct with all 6 timeout fields, figment-based AgentConfig::load(), validation"
      contains: "TimeoutsConfig"
    - path: "pam-unix-oidc/src/audit.rs"
      provides: "get_hostname() using gethostname::gethostname() syscall"
      contains: "gethostname::gethostname()"
    - path: "pam-unix-oidc/src/oidc/jwks.rs"
      provides: "Configurable HTTP timeout and cache TTL from AgentConfig"
      contains: "http_timeout_secs"
  key_links:
    - from: "unix-oidc-agent/src/config.rs"
      to: "figment"
      via: "Figment::from() with Yaml + Env providers"
      pattern: "Figment::from"
    - from: "unix-oidc-agent/src/config.rs"
      to: "pam-unix-oidc/src/oidc/jwks.rs"
      via: "jwks_http_timeout_secs and jwks_cache_ttl_secs passed to JwksProvider constructor"
      pattern: "jwks_http_timeout_secs|jwks_cache_ttl_secs"
    - from: "unix-oidc-agent/src/config.rs"
      to: "unix-oidc-agent/src/main.rs"
      via: "device_flow_http_timeout_secs passed to reqwest client builders"
      pattern: "device_flow_http_timeout_secs"
    - from: "unix-oidc-agent/src/config.rs"
      to: "pam-unix-oidc/src/oidc/dpop.rs"
      via: "clock_skew_future_secs and clock_skew_staleness_secs wired to DPoPConfig"
      pattern: "clock_skew_future_secs|max_proof_age"
---

<objective>
Migrate AgentConfig to figment-based layered loading, add the TimeoutsConfig section with all operator-tunable timeout fields, wire those values to all consumer call sites, and replace the env-var-based hostname resolution in audit.rs with the POSIX gethostname(2) syscall.

Purpose: Foundation for all subsequent plans — configurable timeouts are consumed by JWKS fetching, device flow HTTP clients, DPoP proof validation, and IPC idle timeout. Hostname fix ensures reliable audit logging in containers.
Output: Updated config.rs with figment loading + TimeoutsConfig, updated audit.rs with gethostname syscall, all timeout consumers wired to config values instead of hardcoded constants.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-operational-hardening/13-CONTEXT.md
@.planning/phases/13-operational-hardening/13-RESEARCH.md

<interfaces>
<!-- Existing AgentConfig consumed by main.rs -->
From unix-oidc-agent/src/config.rs:
```rust
pub struct AgentConfig {
    pub issuer: String,
    pub client_id: String,
    pub socket_path: Option<PathBuf>,
    pub crypto: CryptoConfig,
}
impl AgentConfig {
    pub fn from_env() -> Result<Self, ConfigError>;
    pub fn from_file(path: &Path) -> Result<Self, ConfigError>;
    pub fn default_config_path() -> PathBuf;
    pub fn load() -> Result<Self, ConfigError>;
}
```

From pam-unix-oidc/src/audit.rs (line 427-431):
```rust
fn get_hostname() -> String {
    std::env::var("HOSTNAME")
        .or_else(|_| std::env::var("HOST"))
        .unwrap_or_else(|_| "unknown".to_string())
}
```

From pam-unix-oidc/src/oidc/jwks.rs:
```rust
const DEFAULT_CACHE_TTL_SECS: u64 = 300;
const HTTP_TIMEOUT_SECS: u64 = 10;
// Used in two reqwest::blocking::Client::builder().timeout(Duration::from_secs(HTTP_TIMEOUT_SECS)) calls
// JwksProvider::with_cache_ttl(issuer, ttl_secs) already exists
```

From pam-unix-oidc/src/oidc/dpop.rs:
```rust
pub struct DPoPConfig {
    pub max_proof_age: u64,       // maps to clock_skew_staleness_secs
    pub require_nonce: bool,
    pub expected_nonce: Option<String>,
    pub expected_method: String,
    pub expected_uri: String,
}
// Line 308: hardcoded `claims.iat > now + 5` — the 5 maps to clock_skew_future_secs
```

From pam-unix-oidc/src/oidc/validation.rs:
```rust
const CLOCK_SKEW_TOLERANCE: i64 = 60;  // maps to clock_skew_staleness_secs
```

From unix-oidc-agent/src/main.rs:
```rust
// Line 460: .timeout(Duration::from_secs(30)) — device flow discovery client
// Line 824: .timeout(Duration::from_secs(30)) — token refresh client
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TimeoutsConfig struct and migrate AgentConfig to figment</name>
  <files>unix-oidc-agent/Cargo.toml, unix-oidc-agent/src/config.rs</files>
  <behavior>
    - Test: TimeoutsConfig::default() returns correct defaults (jwks_http=10, device_flow=30, skew_future=5, skew_stale=60, jwks_ttl=300, ipc_idle=60)
    - Test: TimeoutsConfig::validate() rejects zero jwks_http_timeout_secs
    - Test: TimeoutsConfig::validate() rejects zero device_flow_http_timeout_secs
    - Test: TimeoutsConfig::validate() rejects jwks_cache_ttl_secs < jwks_http_timeout_secs
    - Test: TimeoutsConfig::validate() rejects clock_skew_future_secs > clock_skew_staleness_secs
    - Test: AgentConfig::load() from YAML file with [timeouts] section parses correctly
    - Test: AgentConfig::load() from YAML without [timeouts] uses defaults (backward compat)
    - Test: UNIX_OIDC_JWKS_CACHE_TTL env var overrides jwks_cache_ttl_secs from file
    - Test: UNIX_OIDC_TIMEOUTS__JWKS_HTTP_TIMEOUT_SECS env var overrides nested timeout
  </behavior>
  <action>
    1. Add figment dependency to unix-oidc-agent/Cargo.toml: `figment = { version = "0.10", features = ["yaml", "env"] }`
    2. In config.rs, add TimeoutsConfig struct with 6 fields (jwks_http_timeout_secs: u64 default 10, device_flow_http_timeout_secs: u64 default 30, clock_skew_future_secs: u64 default 5, clock_skew_staleness_secs: u64 default 60, jwks_cache_ttl_secs: u64 default 300, ipc_idle_timeout_secs: u64 default 60). Each with serde(default = "fn") for the default value.
    3. Add `pub timeouts: TimeoutsConfig` field to AgentConfig with `#[serde(default)]`.
    4. Implement TimeoutsConfig::validate() — reject zero values for http timeouts, jwks_cache_ttl < jwks_http_timeout, clock_skew_future > clock_skew_staleness.
    5. Add Validation variant to ConfigError: `#[error("Configuration validation error: {0}")] Validation(String)`.
    6. Replace AgentConfig::load() with figment-based loading per RESEARCH.md Pattern 6:
       - Figment::from(Serialized::defaults(AgentConfig::default())) to set defaults
       - Merge Yaml::file(config_path) if exists
       - Merge Env::prefixed("UNIX_OIDC_").split("__").only([...]) for env overrides
       - Legacy: check UNIX_OIDC_JWKS_CACHE_TTL env var as direct override
       - Call config.timeouts.validate() before returning
    7. Implement Default for AgentConfig (issuer defaults to empty string — load() should validate non-empty issuer).
    8. Keep from_file() and from_env() as deprecated but functional for backward compat. They can delegate to the new figment path or remain as-is.
    9. Update existing tests. Add new tests for all validation rules and env var overrides.

    Use parking_lot::Mutex for any test env var isolation (ENV_MUTEX pattern from Phase 6 if needed).
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p unix-oidc-agent config:: 2>&1 | tail -30</automated>
  </verify>
  <done>TimeoutsConfig with 6 fields, figment-based loading, validation rejects bad values, env var overrides work, backward compat with existing YAML configs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace get_hostname() with gethostname(2) syscall</name>
  <files>pam-unix-oidc/src/audit.rs</files>
  <behavior>
    - Test: get_hostname() returns a non-empty string (from gethostname syscall)
    - Test: UNIX_OIDC_HOSTNAME env var overrides the syscall result
    - Test: Without UNIX_OIDC_HOSTNAME set, result matches gethostname::gethostname()
  </behavior>
  <action>
    1. In pam-unix-oidc/src/audit.rs, replace get_hostname() at line 427-431:
       - Check UNIX_OIDC_HOSTNAME env var first (operator override for CNAME scenarios)
       - If not set, call gethostname::gethostname().to_string_lossy().into_owned()
       - The gethostname crate is already in pam-unix-oidc/Cargo.toml — no dep addition needed
    2. Add unit tests in the existing audit.rs test module (or create one if none exists).
       - Test with UNIX_OIDC_HOSTNAME set verifies override works
       - Test without env var verifies gethostname returns non-empty hostname

    Do NOT change the function signature — it remains `fn get_hostname() -> String` (module-private).
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p pam-unix-oidc audit:: 2>&1 | tail -20</automated>
  </verify>
  <done>get_hostname() uses gethostname(2) syscall as primary source; UNIX_OIDC_HOSTNAME env var works as override; env var fallback to HOSTNAME/HOST removed</done>
</task>

<task type="auto">
  <name>Task 3: Wire timeout config values to all consumer call sites</name>
  <files>pam-unix-oidc/src/oidc/jwks.rs, pam-unix-oidc/src/oidc/dpop.rs, pam-unix-oidc/src/oidc/validation.rs, unix-oidc-agent/src/main.rs</files>
  <action>
    Wire all 5 timeout fields from TimeoutsConfig to the code that enforces them. Each wiring replaces a hardcoded constant.

    **1. jwks_http_timeout_secs → pam-unix-oidc/src/oidc/jwks.rs**
    - Add `http_timeout: Duration` field to `JwksProvider` struct.
    - Add `pub fn with_http_timeout(issuer: &str, ttl_secs: u64, http_timeout_secs: u64) -> Self` constructor (or extend `with_cache_ttl` to also accept http_timeout).
    - Replace both `Duration::from_secs(HTTP_TIMEOUT_SECS)` usages in `fetch_discovery()` and `fetch_jwks()` with `self.http_timeout`.
    - Keep `HTTP_TIMEOUT_SECS` as fallback default in `JwksProvider::new()` for backward compat.
    - Update existing tests that construct JwksProvider.

    **2. jwks_cache_ttl_secs → pam-unix-oidc/src/oidc/jwks.rs**
    - Already wired via `JwksProvider::with_cache_ttl()`. Ensure the caller in main.rs passes `config.timeouts.jwks_cache_ttl_secs` when constructing JwksProvider. Find where JwksProvider is instantiated in main.rs or the serve path and pass the config value.

    **3. device_flow_http_timeout_secs → unix-oidc-agent/src/main.rs**
    - At line ~460 (device flow discovery client): replace `.timeout(Duration::from_secs(30))` with `.timeout(Duration::from_secs(config.timeouts.device_flow_http_timeout_secs))`. Thread `config` (or just the timeout value) to this function.
    - At line ~824 (token refresh client): replace `.timeout(Duration::from_secs(30))` with `.timeout(Duration::from_secs(config.timeouts.device_flow_http_timeout_secs))`. Thread the timeout value to the refresh function.

    **4. clock_skew_future_secs → pam-unix-oidc/src/oidc/dpop.rs**
    - Add `pub clock_skew_future_secs: u64` field to `DPoPConfig` with default 5.
    - Replace the hardcoded `claims.iat > now + 5` at line 308 with `claims.iat > now + config.clock_skew_future_secs as i64`.
    - Update all test DPoPConfig constructions to include the new field.

    **5. clock_skew_staleness_secs → pam-unix-oidc/src/oidc/dpop.rs + validation.rs**
    - DPoPConfig::max_proof_age already serves this role. Ensure the caller passes `config.timeouts.clock_skew_staleness_secs` as `max_proof_age` when constructing DPoPConfig.
    - In validation.rs: Replace `const CLOCK_SKEW_TOLERANCE: i64 = 60` with a parameter. Add `clock_skew_tolerance_secs: i64` to the validation function signature (or to whatever config struct it uses). Wire from `config.timeouts.clock_skew_staleness_secs`.
    - If making CLOCK_SKEW_TOLERANCE a parameter is too invasive, make it a module-level `static` that is set from config at initialization, or pass it through the existing config path.

    **Testing:**
    - Verify `cargo test -p pam-unix-oidc` passes (existing dpop and validation tests updated with new fields).
    - Verify `cargo build -p unix-oidc-agent` compiles (main.rs wiring).
    - Add at least one integration-style test: construct AgentConfig with non-default timeout, verify the value reaches the JwksProvider constructor.
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test --workspace 2>&1 | tail -30</automated>
  </verify>
  <done>No hardcoded timeout constants remain — HTTP_TIMEOUT_SECS in jwks.rs only used as default, hardcoded 30s in main.rs replaced, hardcoded 5 in dpop.rs replaced, CLOCK_SKEW_TOLERANCE in validation.rs replaced. All values sourced from TimeoutsConfig.</done>
</task>

</tasks>

<verification>
- `cargo test -p unix-oidc-agent config::` — all config tests pass
- `cargo test -p pam-unix-oidc audit::` — hostname tests pass
- `cargo test -p pam-unix-oidc dpop::` — dpop tests pass with new clock_skew_future_secs field
- `cargo test -p pam-unix-oidc` — all pam tests pass including validation with parameterized clock skew
- `cargo build -p unix-oidc-agent` — compiles clean with figment dependency and timeout wiring
</verification>

<success_criteria>
- TimeoutsConfig struct exists with 6 configurable timeout fields and sensible defaults
- AgentConfig::load() uses figment for layered YAML + env var loading
- Validation rejects nonsensical timeout combinations
- UNIX_OIDC_JWKS_CACHE_TTL env var overrides jwks_cache_ttl_secs
- get_hostname() calls gethostname(2) instead of reading env vars
- jwks_http_timeout_secs wired to JwksProvider HTTP client (replaces HTTP_TIMEOUT_SECS constant)
- device_flow_http_timeout_secs wired to device flow and token refresh HTTP clients (replaces hardcoded 30s)
- clock_skew_future_secs wired to DPoP future-proof check (replaces hardcoded 5)
- clock_skew_staleness_secs wired to DPoPConfig::max_proof_age and CLOCK_SKEW_TOLERANCE (replaces hardcoded 60)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-operational-hardening/13-01-SUMMARY.md`
</output>
