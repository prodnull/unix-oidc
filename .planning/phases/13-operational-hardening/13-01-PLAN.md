---
phase: 13-operational-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - unix-oidc-agent/Cargo.toml
  - unix-oidc-agent/src/config.rs
  - pam-unix-oidc/src/audit.rs
autonomous: true
requirements: [OPS-07, OPS-08, OPS-09, OPS-10, OPS-12]

must_haves:
  truths:
    - "Operator can set jwks_cache_ttl_secs in config.yaml and the value is used for JWKS cache TTL"
    - "UNIX_OIDC_JWKS_CACHE_TTL env var overrides the config file value"
    - "Zero or negative timeout values cause config load to fail with a clear error"
    - "get_hostname() returns the POSIX gethostname(2) result, not an environment variable"
  artifacts:
    - path: "unix-oidc-agent/src/config.rs"
      provides: "TimeoutsConfig struct with all 6 timeout fields, figment-based AgentConfig::load(), validation"
      contains: "TimeoutsConfig"
    - path: "pam-unix-oidc/src/audit.rs"
      provides: "get_hostname() using gethostname::gethostname() syscall"
      contains: "gethostname::gethostname()"
  key_links:
    - from: "unix-oidc-agent/src/config.rs"
      to: "figment"
      via: "Figment::from() with Yaml + Env providers"
      pattern: "Figment::from"
---

<objective>
Migrate AgentConfig to figment-based layered loading and add the TimeoutsConfig section with all operator-tunable timeout fields. Also replace the env-var-based hostname resolution in audit.rs with the POSIX gethostname(2) syscall.

Purpose: Foundation for all subsequent plans — configurable timeouts are consumed by socket acquisition, IPC idle timeout, and JWKS fetching. Hostname fix ensures reliable audit logging in containers.
Output: Updated config.rs with figment loading + TimeoutsConfig, updated audit.rs with gethostname syscall.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-operational-hardening/13-CONTEXT.md
@.planning/phases/13-operational-hardening/13-RESEARCH.md

<interfaces>
<!-- Existing AgentConfig consumed by main.rs -->
From unix-oidc-agent/src/config.rs:
```rust
pub struct AgentConfig {
    pub issuer: String,
    pub client_id: String,
    pub socket_path: Option<PathBuf>,
    pub crypto: CryptoConfig,
}
impl AgentConfig {
    pub fn from_env() -> Result<Self, ConfigError>;
    pub fn from_file(path: &Path) -> Result<Self, ConfigError>;
    pub fn default_config_path() -> PathBuf;
    pub fn load() -> Result<Self, ConfigError>;
}
```

From pam-unix-oidc/src/audit.rs (line 427-431):
```rust
fn get_hostname() -> String {
    std::env::var("HOSTNAME")
        .or_else(|_| std::env::var("HOST"))
        .unwrap_or_else(|_| "unknown".to_string())
}
```

From pam-unix-oidc/src/oidc/jwks.rs:
```rust
const DEFAULT_CACHE_TTL_SECS: u64 = 300;
const HTTP_TIMEOUT_SECS: u64 = 10;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TimeoutsConfig struct and migrate AgentConfig to figment</name>
  <files>unix-oidc-agent/Cargo.toml, unix-oidc-agent/src/config.rs</files>
  <behavior>
    - Test: TimeoutsConfig::default() returns correct defaults (jwks_http=10, device_flow=30, skew_future=5, skew_stale=60, jwks_ttl=300, ipc_idle=60)
    - Test: TimeoutsConfig::validate() rejects zero jwks_http_timeout_secs
    - Test: TimeoutsConfig::validate() rejects zero device_flow_http_timeout_secs
    - Test: TimeoutsConfig::validate() rejects jwks_cache_ttl_secs < jwks_http_timeout_secs
    - Test: TimeoutsConfig::validate() rejects clock_skew_future_secs > clock_skew_staleness_secs
    - Test: AgentConfig::load() from YAML file with [timeouts] section parses correctly
    - Test: AgentConfig::load() from YAML without [timeouts] uses defaults (backward compat)
    - Test: UNIX_OIDC_JWKS_CACHE_TTL env var overrides jwks_cache_ttl_secs from file
    - Test: UNIX_OIDC_TIMEOUTS__JWKS_HTTP_TIMEOUT_SECS env var overrides nested timeout
  </behavior>
  <action>
    1. Add figment dependency to unix-oidc-agent/Cargo.toml: `figment = { version = "0.10", features = ["yaml", "env"] }`
    2. In config.rs, add TimeoutsConfig struct with 6 fields (jwks_http_timeout_secs: u64 default 10, device_flow_http_timeout_secs: u64 default 30, clock_skew_future_secs: u64 default 5, clock_skew_staleness_secs: u64 default 60, jwks_cache_ttl_secs: u64 default 300, ipc_idle_timeout_secs: u64 default 60). Each with serde(default = "fn") for the default value.
    3. Add `pub timeouts: TimeoutsConfig` field to AgentConfig with `#[serde(default)]`.
    4. Implement TimeoutsConfig::validate() — reject zero values for http timeouts, jwks_cache_ttl < jwks_http_timeout, clock_skew_future > clock_skew_staleness.
    5. Add Validation variant to ConfigError: `#[error("Configuration validation error: {0}")] Validation(String)`.
    6. Replace AgentConfig::load() with figment-based loading per RESEARCH.md Pattern 6:
       - Figment::from(Serialized::defaults(AgentConfig::default())) to set defaults
       - Merge Yaml::file(config_path) if exists
       - Merge Env::prefixed("UNIX_OIDC_").split("__").only([...]) for env overrides
       - Legacy: check UNIX_OIDC_JWKS_CACHE_TTL env var as direct override
       - Call config.timeouts.validate() before returning
    7. Implement Default for AgentConfig (issuer defaults to empty string — load() should validate non-empty issuer).
    8. Keep from_file() and from_env() as deprecated but functional for backward compat. They can delegate to the new figment path or remain as-is.
    9. Update existing tests. Add new tests for all validation rules and env var overrides.

    Use parking_lot::Mutex for any test env var isolation (ENV_MUTEX pattern from Phase 6 if needed).
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p unix-oidc-agent config:: 2>&1 | tail -30</automated>
  </verify>
  <done>TimeoutsConfig with 6 fields, figment-based loading, validation rejects bad values, env var overrides work, backward compat with existing YAML configs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace get_hostname() with gethostname(2) syscall</name>
  <files>pam-unix-oidc/src/audit.rs</files>
  <behavior>
    - Test: get_hostname() returns a non-empty string (from gethostname syscall)
    - Test: UNIX_OIDC_HOSTNAME env var overrides the syscall result
    - Test: Without UNIX_OIDC_HOSTNAME set, result matches gethostname::gethostname()
  </behavior>
  <action>
    1. In pam-unix-oidc/src/audit.rs, replace get_hostname() at line 427-431:
       - Check UNIX_OIDC_HOSTNAME env var first (operator override for CNAME scenarios)
       - If not set, call gethostname::gethostname().to_string_lossy().into_owned()
       - The gethostname crate is already in pam-unix-oidc/Cargo.toml — no dep addition needed
    2. Add unit tests in the existing audit.rs test module (or create one if none exists).
       - Test with UNIX_OIDC_HOSTNAME set verifies override works
       - Test without env var verifies gethostname returns non-empty hostname

    Do NOT change the function signature — it remains `fn get_hostname() -> String` (module-private).
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p pam-unix-oidc audit:: 2>&1 | tail -20</automated>
  </verify>
  <done>get_hostname() uses gethostname(2) syscall as primary source; UNIX_OIDC_HOSTNAME env var works as override; env var fallback to HOSTNAME/HOST removed</done>
</task>

</tasks>

<verification>
- `cargo test -p unix-oidc-agent config::` — all config tests pass
- `cargo test -p pam-unix-oidc audit::` — hostname tests pass
- `cargo build -p unix-oidc-agent` — compiles clean with figment dependency
</verification>

<success_criteria>
- TimeoutsConfig struct exists with 6 configurable timeout fields and sensible defaults
- AgentConfig::load() uses figment for layered YAML + env var loading
- Validation rejects nonsensical timeout combinations
- UNIX_OIDC_JWKS_CACHE_TTL env var overrides jwks_cache_ttl_secs
- get_hostname() calls gethostname(2) instead of reading env vars
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-operational-hardening/13-01-SUMMARY.md`
</output>
