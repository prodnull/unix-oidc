---
phase: 26-tech-debt-resolution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scripts/get-entra-token.sh
  - test/scripts/test-entra-diagnostic.sh
  - unix-oidc-agent/src/storage/secure_delete.rs
  - CLAUDE.md
autonomous: true
requirements: [DEBT-06, DEBT-08]

must_haves:
  truths:
    - "Entra ROPC script logs a diagnostic message when Conditional Access blocks ROPC (AADSTS50076, AADSTS53003, interaction_required)"
    - "Entra ROPC script still exits non-zero on failure (diagnostic does not mask the error)"
    - "secure_delete.rs cites NIST SP 800-88 Rev 1 as primary reference"
    - "DoD 5220.22-M appears only as a historical note in secure_delete.rs"
    - "CLAUDE.md citations updated to match source code"
  artifacts:
    - path: "test/scripts/get-entra-token.sh"
      provides: "Conditional Access diagnostic in ROPC error path"
      contains: "AADSTS50076"
    - path: "test/scripts/test-entra-diagnostic.sh"
      provides: "Mock-based bash unit tests for diagnostic trigger"
      contains: "interaction_required"
    - path: "unix-oidc-agent/src/storage/secure_delete.rs"
      provides: "NIST SP 800-88 Rev 1 as primary citation"
      contains: "NIST SP 800-88 Rev 1"
    - path: "CLAUDE.md"
      provides: "Updated citation from DoD to NIST SP 800-88"
      contains: "NIST SP 800-88 Rev 1"
  key_links:
    - from: "test/scripts/get-entra-token.sh"
      to: "Entra ROPC endpoint"
      via: "curl error response parsing"
      pattern: "AADSTS|interaction_required"
---

<objective>
Improve Entra CI ROPC diagnostic messaging when Conditional Access blocks ROPC (DEBT-06) and update secure_delete.rs citations from DoD 5220.22-M to NIST SP 800-88 Rev 1 (DEBT-08).

Purpose: DEBT-06 — when Entra Conditional Access blocks ROPC, the CI failure is opaque. Adding diagnostic parsing helps operators identify and fix the issue. DEBT-08 — DoD 5220.22-M was retired by DoD in 2006; NIST SP 800-88 Rev 1 is the current authoritative reference for media sanitization.

Output: Better CI diagnostics for Entra failures; accurate standards citations throughout codebase.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-tech-debt-resolution/26-CONTEXT.md

<interfaces>
<!-- Current Entra ROPC script error handling (get-entra-token.sh lines 46-55) -->
```bash
ACCESS_TOKEN=$(echo "${RESPONSE}" | jq -r '.access_token // empty')

if [ -z "${ACCESS_TOKEN}" ]; then
  ERROR=$(echo "${RESPONSE}" | jq -r '.error_description // .error // "unknown error"')
  echo "ERROR: Failed to acquire Entra token: ${ERROR}" >&2
  exit 1
fi
```

<!-- Current secure_delete.rs header (already mostly updated, verify completeness) -->
```rust
//! Implements a three-pass overwrite for file deletion following NIST SP 800-88
//! Rev 1 "Guidelines for Media Sanitization" SS2.4 (Clear method):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Conditional Access diagnostic to Entra ROPC script with mock tests</name>
  <files>test/scripts/get-entra-token.sh, test/scripts/test-entra-diagnostic.sh</files>
  <action>
1. **Modify the error handling block** in `get-entra-token.sh` (after the `if [ -z "${ACCESS_TOKEN}" ]` check):
   - Before the generic error message, check the response for Conditional Access indicators
   - Parse the error and error_description fields from the JSON response
   - Check for known Conditional Access error codes:
     - `AADSTS50076` — MFA required but not provided (user needs MFA exclusion)
     - `AADSTS53003` — Access blocked by Conditional Access policy
     - `AADSTS50079` — User needs to register for MFA
     - `interaction_required` — Generic interactive auth needed (ROPC can't handle)
   - When detected, log an actionable diagnostic BEFORE the generic error:
     ```bash
     echo "DIAGNOSTIC: Entra ROPC failed due to Conditional Access policy." >&2
     echo "  Possible causes:" >&2
     echo "  - MFA required for test user (exclude from Conditional Access or use MFA-exempt user)" >&2
     echo "  - Conditional Access blocks ROPC grant type (add Named Location exclusion for CI IP range)" >&2
     echo "  - Consider switching to client_credentials grant if user context is not required" >&2
     echo "  Error code: ${ERROR_CODE}" >&2
     ```
   - The script MUST still exit 1 after the diagnostic — do NOT mask the failure

2. **Refactor the diagnostic logic into a testable function** within `get-entra-token.sh`:
   - Extract the Conditional Access detection into a bash function `check_conditional_access_error()` that takes the JSON response as an argument
   - The function should echo the diagnostic to stderr and return 0 if CA error detected, 1 if not
   - This makes the logic sourceable for testing

3. **Modify curl call** to capture the full response even on HTTP error:
   - Change `curl -sf` to `curl -s` (remove `-f` so we get the error response body)
   - After the curl call, check if `ACCESS_TOKEN` is empty to detect failure (already done)
   - This ensures we have the JSON error response to parse

4. **Create mock-based bash unit test** at `test/scripts/test-entra-diagnostic.sh`:
   - The test script sources the diagnostic function from `get-entra-token.sh` (or duplicates the detection logic if sourcing is impractical due to the script's main body executing on source)
   - **Test case 1 — CA error triggers diagnostic**: Inject fake JSON `{"error":"interaction_required","error_description":"AADSTS50076: Due to a configuration change..."}` into the detection function. Confirm: diagnostic message is emitted to stderr, function/script exits non-zero.
   - **Test case 2 — Valid token does NOT trigger diagnostic**: Inject fake JSON `{"access_token":"fake-token-value","token_type":"Bearer"}`. Confirm: no diagnostic output, no error exit.
   - **Test case 3 — Non-CA error does NOT trigger CA diagnostic**: Inject fake JSON `{"error":"invalid_grant","error_description":"AADSTS70002: Invalid client secret"}`. Confirm: generic error path fires but CA diagnostic does NOT fire.
   - Script exits 0 if all tests pass, non-zero on any failure.
   - Make executable: `chmod +x test/scripts/test-entra-diagnostic.sh`

5. **Negative behavior**: A successful ROPC response (valid token) should NOT trigger the diagnostic path. Only triggered when ACCESS_TOKEN is empty AND the error matches Conditional Access patterns.
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && bash -n test/scripts/get-entra-token.sh && grep -q "AADSTS50076" test/scripts/get-entra-token.sh && grep -q "AADSTS53003" test/scripts/get-entra-token.sh && bash test/scripts/test-entra-diagnostic.sh && echo "PASS: Script syntax valid, CA patterns present, mock tests pass"</automated>
  </verify>
  <done>Entra ROPC script detects Conditional Access error codes (AADSTS50076, AADSTS53003, AADSTS50079, interaction_required) and logs actionable diagnostic; script still exits non-zero on failure; bash syntax validates clean; mock-based tests confirm diagnostic fires on CA errors, does not fire on valid tokens or non-CA errors</done>
</task>

<task type="auto">
  <name>Task 2: Update secure_delete.rs and CLAUDE.md citations from DoD to NIST SP 800-88</name>
  <files>unix-oidc-agent/src/storage/secure_delete.rs, CLAUDE.md</files>
  <action>
1. **Verify secure_delete.rs current state** — read the file to confirm current citations. Based on grep, it already references NIST SP 800-88 Rev 1 as primary. Verify no remaining DoD 5220.22-M references exist as primary citations. If DoD is already only historical, this file may need minimal changes.

2. **Update CLAUDE.md** — line 198 currently says:
   ```
   7. **File deletion uses three-pass DoD 5220.22-M overwrite** (MEM-05)
   ```
   Change to:
   ```
   7. **File deletion uses three-pass overwrite per NIST SP 800-88 Rev 1 SS2.4** (MEM-05)
   ```
   Add a historical note below the description:
   ```
   - Originally inspired by DoD 5220.22-M (retired by DoD in 2006). Current implementation follows NIST SP 800-88 Rev 1 SS2.4 (Clear method) as the authoritative media sanitization guidance.
   ```

3. **Check secure_delete.rs doc comments** — if the file already cites NIST SP 800-88 as primary (based on grep, it does), verify:
   - Module-level doc comment references NIST SP 800-88 Rev 1 SS2.4 (verified: it does)
   - No remaining "DoD 5220.22-M" as a primary citation (if any remain, change to historical note)
   - Add a historical note if not present: `//! Historical note: Originally inspired by DoD 5220.22-M, retired by DoD in 2006.`

4. **Check other source files** for DoD references that should be updated:
   - `unix-oidc-agent/src/storage/file_store.rs` — check for DoD references
   - `unix-oidc-agent/src/storage/router.rs` — check for DoD references
   - Update any found to reference NIST SP 800-88 Rev 1

5. **No functional code changes** — only doc comments and citations change. The three-pass overwrite implementation stays identical.

6. **Verification**: grep the entire codebase for "DoD 5220" — should appear ONLY as historical notes, never as the primary citation for the overwrite method.
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && MATCHES=$(grep -rn "DoD 5220" unix-oidc-agent/src/ CLAUDE.md | grep -v "historical\|retired\|Originally\|superseded\|Historical" | wc -l); if [ "$MATCHES" -gt 0 ]; then echo "FAIL: $MATCHES non-historical DoD 5220 references remain"; exit 1; else echo "PASS: DoD 5220 appears only as historical note"; fi</automated>
  </verify>
  <done>NIST SP 800-88 Rev 1 SS2.4 is the primary citation in secure_delete.rs and CLAUDE.md; DoD 5220.22-M appears only as a historical note; no functional code changes; cargo build passes</done>
</task>

</tasks>

<verification>
- `bash -n test/scripts/get-entra-token.sh` — script syntax validates
- `bash test/scripts/test-entra-diagnostic.sh` — mock tests pass (CA error triggers diagnostic, valid token does not, non-CA error does not trigger CA diagnostic)
- `grep "AADSTS" test/scripts/get-entra-token.sh` — Conditional Access error codes present
- DoD 5220 grep with wc -l pattern confirms zero non-historical references
- `cargo build -p unix-oidc-agent` — builds clean (no source code changes, only doc comments)
</verification>

<success_criteria>
1. Entra ROPC script detects AADSTS50076, AADSTS53003, AADSTS50079, interaction_required and logs actionable diagnostic
2. Script still exits non-zero on any failure (diagnostic is informational, not error-masking)
3. Mock-based bash tests confirm diagnostic fires correctly on CA errors and does not fire on valid tokens
4. NIST SP 800-88 Rev 1 SS2.4 is the primary citation everywhere
5. DoD 5220.22-M appears only as historical context
6. No functional code changes in secure_delete.rs
</success_criteria>

<output>
After completion, create `.planning/phases/26-tech-debt-resolution/26-03-SUMMARY.md`
</output>
