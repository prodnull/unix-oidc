---
phase: 01-memory-protection-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - unix-oidc-agent/Cargo.toml
  - Cargo.toml
  - unix-oidc-agent/src/crypto/protected_key.rs
  - unix-oidc-agent/src/crypto/mod.rs
  - unix-oidc-agent/src/crypto/signer.rs
autonomous: true
requirements: [MEM-01, MEM-02, MEM-04, MEM-05]

must_haves:
  truths:
    - "SigningKey is zeroed from memory when ProtectedSigningKey is dropped"
    - "Key material pages are locked via mlock when available"
    - "export_key() returns Zeroizing<Vec<u8>> not raw Vec<u8>"
    - "ProtectedSigningKey can only be constructed as Box<ProtectedSigningKey> -- no stack copies"
    - "import_key() accepts Zeroizing<Vec<u8>> and key bytes are immediately mlocked"
  artifacts:
    - path: "unix-oidc-agent/src/crypto/protected_key.rs"
      provides: "ProtectedSigningKey with ZeroizeOnDrop, mlock, Box-only constructor"
      min_lines: 80
    - path: "unix-oidc-agent/Cargo.toml"
      provides: "zeroize, secrecy, libc dependencies; p256 zeroize feature enabled"
      contains: 'features = ["ecdsa", "jwk", "zeroize"]'
  key_links:
    - from: "unix-oidc-agent/src/crypto/protected_key.rs"
      to: "p256::ecdsa::SigningKey"
      via: "wraps SigningKey with ZeroizeOnDrop derive"
      pattern: "ZeroizeOnDrop|zeroize"
    - from: "unix-oidc-agent/src/crypto/signer.rs"
      to: "unix-oidc-agent/src/crypto/protected_key.rs"
      via: "SoftwareSigner uses ProtectedSigningKey internally"
      pattern: "ProtectedSigningKey"
---

<objective>
Add zeroize/secrecy/libc dependencies to the workspace, enable p256 zeroize feature, and create the ProtectedSigningKey wrapper that provides ZeroizeOnDrop + mlock for DPoP signing keys. Wire ProtectedSigningKey into SoftwareSigner, changing export_key to return Zeroizing<Vec<u8>> and import_key to accept bytes that get immediately mlocked.

Purpose: Foundation for all memory protection -- every downstream plan depends on these types existing.
Output: ProtectedSigningKey module, updated SoftwareSigner, updated Cargo.toml dependencies.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-protection-hardening/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From unix-oidc-agent/src/crypto/signer.rs:
```rust
pub trait DPoPSigner: Send + Sync {
    fn thumbprint(&self) -> String;
    fn sign_proof(&self, method: &str, target: &str, nonce: Option<&str>) -> Result<String, DPoPError>;
    fn public_key_jwk(&self) -> serde_json::Value;
}

pub struct SoftwareSigner {
    signing_key: SigningKey,
    thumbprint: String,
}

impl SoftwareSigner {
    pub fn generate() -> Self;
    pub fn from_key(signing_key: SigningKey) -> Self;
    pub fn export_key(&self) -> Vec<u8>;  // CHANGES to Zeroizing<Vec<u8>>
    pub fn import_key(bytes: &[u8]) -> Result<Self, SignerError>;
}
```

From unix-oidc-agent/src/crypto/mod.rs:
```rust
pub use signer::{DPoPSigner, SignerError, SoftwareSigner};
```

From unix-oidc-agent/src/main.rs (callers of export_key/import_key):
```rust
// line 763: storage.store(KEY_DPOP_PRIVATE, &signer.export_key())?;
// line 758: SoftwareSigner::import_key(&key_bytes)
```

From unix-oidc-agent/Cargo.toml:
```toml
p256 = { version = "0.13", features = ["ecdsa", "jwk"] }
# libc is NOT currently a direct dependency of unix-oidc-agent
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies and create ProtectedSigningKey module</name>
  <files>unix-oidc-agent/Cargo.toml, Cargo.toml, unix-oidc-agent/src/crypto/protected_key.rs, unix-oidc-agent/src/crypto/mod.rs</files>
  <behavior>
    - Test: ProtectedSigningKey::new() returns Box&lt;ProtectedSigningKey&gt; (not stack-constructible)
    - Test: ProtectedSigningKey::generate() creates a valid key with non-empty thumbprint
    - Test: export_key() returns Zeroizing&lt;Vec&lt;u8&gt;&gt; that round-trips through import_key()
    - Test: After dropping ProtectedSigningKey, key material is zeroed (unsafe memory inspection in test is acceptable per Claude's Discretion)
    - Test: mlock_probe() returns MlockStatus indicating whether mlock is available (best-effort, never fatal)
  </behavior>
  <action>
    1. Update unix-oidc-agent/Cargo.toml:
       - Change p256 features to ["ecdsa", "jwk", "zeroize"] (MEM-02: enables ZeroizeOnDrop on SigningKey)
       - Add `zeroize = { version = "1", features = ["derive"] }`
       - Add `secrecy = "0.10"` (needed by Plan 02 but add now so workspace is ready)
       - Add `libc = "0.2"` (for mlock/prctl syscalls)

    2. Create unix-oidc-agent/src/crypto/protected_key.rs:
       - Define `MlockStatus` enum: Active, Unavailable(String)
       - Define `MlockGuard` struct that holds a raw pointer + length, calls munlock on Drop
       - Implement `mlock_probe()` function: allocate a test page, try libc::mlock, munlock, return MlockStatus. Log at INFO level. On EPERM/ENOMEM warn with mitigation guidance ("increase RLIMIT_MEMLOCK or run as root"). Never fatal.
       - Define `ProtectedSigningKey` struct containing:
         - `signing_key: SigningKey` (with p256 zeroize feature, this gets ZeroizeOnDrop)
         - `thumbprint: String`
         - `_mlock_guard: Option<MlockGuard>` (Option per Claude's Discretion)
       - Private constructor `fn new_inner(signing_key: SigningKey) -> Box<Self>`:
         - Compute thumbprint via compute_ec_thumbprint
         - Box the struct first (heap allocation), then attempt mlock on the key bytes within the Box
         - Return Box<ProtectedSigningKey>
       - Public API:
         - `pub fn generate() -> Box<Self>` -- random key via OsRng, calls new_inner
         - `pub fn from_bytes(bytes: &[u8]) -> Result<Box<Self>, SignerError>` -- parse SigningKey from slice, calls new_inner
         - `pub fn export_key(&self) -> Zeroizing<Vec<u8>>` -- returns Zeroizing<Vec<u8>> wrapping signing_key.to_bytes().to_vec() (MEM-01)
         - `pub fn thumbprint(&self) -> &str`
         - `pub fn signing_key(&self) -> &SigningKey` -- needed by SoftwareSigner for DPoP proof generation
         - `pub fn verifying_key(&self) -> &p256::ecdsa::VerifyingKey`
       - NO public `new()` that returns Self on the stack -- only Box<Self> constructors (MEM-05)

    3. Update unix-oidc-agent/src/crypto/mod.rs:
       - Add `pub mod protected_key;`
       - Add `pub use protected_key::{ProtectedSigningKey, MlockStatus};`
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p unix-oidc-agent --lib crypto::protected_key -- --nocapture</automated>
  </verify>
  <done>ProtectedSigningKey module exists with Box-only constructors, ZeroizeOnDrop on inner SigningKey, mlock guard, and Zeroizing export. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ProtectedSigningKey into SoftwareSigner</name>
  <files>unix-oidc-agent/src/crypto/signer.rs, unix-oidc-agent/src/main.rs</files>
  <behavior>
    - Test: SoftwareSigner::generate() still produces valid thumbprints (43 chars)
    - Test: SoftwareSigner export/import round-trip works with Zeroizing&lt;Vec&lt;u8&gt;&gt;
    - Test: SoftwareSigner implements DPoPSigner (sign_proof produces valid JWT)
    - Test: SoftwareSigner is Send + Sync
  </behavior>
  <action>
    1. Refactor SoftwareSigner in unix-oidc-agent/src/crypto/signer.rs:
       - Change internal field from `signing_key: SigningKey` to `key: Box<ProtectedSigningKey>`
       - Remove `thumbprint: String` field (ProtectedSigningKey owns it)
       - Update `generate()`: call ProtectedSigningKey::generate(), return Self { key }
       - Update `from_key(signing_key: SigningKey)`: call ProtectedSigningKey new_inner equivalent. Since new_inner is private, add a `from_signing_key` constructor on ProtectedSigningKey or make from_key accept SigningKey directly. Simplest: keep a pub(crate) constructor path.
       - Change `export_key(&self) -> Vec<u8>` to `export_key(&self) -> Zeroizing<Vec<u8>>` (MEM-01)
       - Change `import_key(bytes: &[u8])` to `import_key(bytes: &[u8]) -> Result<Self, SignerError>` -- internally uses ProtectedSigningKey::from_bytes
       - Update DPoPSigner impl: thumbprint() delegates to self.key.thumbprint(), sign_proof delegates using self.key.signing_key(), public_key_jwk uses self.key.verifying_key()
       - Update verifying_key() to delegate

    2. Update callers in unix-oidc-agent/src/main.rs:
       - Line ~763: `storage.store(KEY_DPOP_PRIVATE, &signer.export_key())?;` -- Zeroizing<Vec<u8>> derefs to &[u8] via AsRef, so this should work without changes. Verify.
       - Line ~758: `SoftwareSigner::import_key(&key_bytes)` -- key_bytes is Vec<u8> from storage.retrieve(). This works as &[u8]. Verify.
       - If any type errors arise from the Zeroizing return type, fix them.

    3. Ensure all existing tests in signer.rs still pass. The test_signer_export_import_roundtrip test needs updating since export_key now returns Zeroizing<Vec<u8>> -- the deref to &[u8] should make import_key(&exported) work, but verify.
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p unix-oidc-agent -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>SoftwareSigner uses ProtectedSigningKey internally. export_key returns Zeroizing&lt;Vec&lt;u8&gt;&gt;. All existing tests pass including daemon socket tests. cargo build succeeds with no warnings.</done>
</task>

</tasks>

<verification>
- `cargo build -p unix-oidc-agent` succeeds with no warnings
- `cargo test -p unix-oidc-agent` -- all tests pass (unit + integration)
- `cargo clippy -p unix-oidc-agent -- -D warnings` -- no clippy warnings
- grep confirms: no raw `Vec<u8>` return from export_key, no stack-constructible ProtectedSigningKey
</verification>

<success_criteria>
- p256 built with zeroize feature (ZeroizeOnDrop on SigningKey)
- ProtectedSigningKey exists with Box-only constructors
- mlock attempted on key material pages (best-effort, logged)
- export_key() returns Zeroizing<Vec<u8>> everywhere
- All existing tests pass after refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-protection-hardening/01-01-SUMMARY.md`
</output>
