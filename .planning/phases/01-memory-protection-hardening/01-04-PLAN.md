---
phase: 01-memory-protection-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - unix-oidc-agent/src/main.rs
  - unix-oidc-agent/src/daemon/socket.rs
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [MEM-03]

must_haves:
  truths:
    - "refresh_token is wrapped in SecretString at extraction time in both main.rs and socket.rs"
    - "client_secret is wrapped in SecretString at extraction time in both main.rs and socket.rs"
    - "expose_secret() is the only path to raw values of refresh_token and client_secret, used only at HTTP param and storage write boundaries"
    - "REQUIREMENTS.md traceability table shows MEM-03 as Complete"
  artifacts:
    - path: "unix-oidc-agent/src/main.rs"
      provides: "SecretString wrapping for refresh_token and client_secret"
      contains: "SecretString"
    - path: "unix-oidc-agent/src/daemon/socket.rs"
      provides: "SecretString wrapping for refresh_token and client_secret in perform_token_refresh"
      contains: "expose_secret"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected MEM-03 traceability status"
      contains: "Complete"
  key_links:
    - from: "unix-oidc-agent/src/main.rs"
      to: "secrecy::SecretString"
      via: "refresh_token wrapped at extraction"
      pattern: "refresh_token.*SecretString"
    - from: "unix-oidc-agent/src/main.rs"
      to: "secrecy::SecretString"
      via: "client_secret wrapped at extraction"
      pattern: "client_secret.*SecretString"
    - from: "unix-oidc-agent/src/daemon/socket.rs"
      to: "secrecy::SecretString"
      via: "refresh_token and client_secret wrapped in perform_token_refresh"
      pattern: "SecretString::from"
---

<objective>
Close the MEM-03 gap: wrap refresh_token and client_secret in SecretString in main.rs and socket.rs, mirroring the pattern already applied to access_token.

Purpose: Complete MEM-03 requirement — all three OAuth credential types (access_token, refresh_token, client_secret) must be wrapped in secrecy::SecretString so they never appear as raw strings in Debug/Display output or tracing logs.

Output: Two source files patched, REQUIREMENTS.md traceability corrected.
</objective>

<execution_context>
@/Users/cbc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cbc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-protection-hardening/01-VERIFICATION.md

<interfaces>
<!-- Existing pattern from access_token wrapping — replicate exactly for refresh_token and client_secret -->

From unix-oidc-agent/src/main.rs (line 10):
```rust
use secrecy::{ExposeSecret, SecretString};
```

Pattern (access_token, line 512-513):
```rust
// Wrap in SecretString immediately — must not appear in logs (MEM-03).
let access_token = SecretString::from(
    token_result["access_token"]
        .as_str()
        .ok_or_else(|| anyhow::anyhow!("No access_token in response"))?
        .to_string(),
);
```

Storage write pattern (line 527-528):
```rust
// Storage write: expose_secret() is the audit boundary for persistence.
storage.store(KEY_ACCESS_TOKEN, access_token.expose_secret().as_bytes())?;
```

From unix-oidc-agent/src/daemon/socket.rs (line 6):
```rust
use secrecy::{ExposeSecret, SecretString};
```

Same wrapping pattern used at socket.rs line 478-479 for access_token in perform_token_refresh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap refresh_token and client_secret in SecretString across main.rs and socket.rs</name>
  <files>unix-oidc-agent/src/main.rs, unix-oidc-agent/src/daemon/socket.rs</files>
  <action>
Apply SecretString wrapping to refresh_token and client_secret in four code locations, mirroring the exact pattern already used for access_token. The imports (`use secrecy::{ExposeSecret, SecretString}`) already exist in both files.

**main.rs — run_login() function (~line 295):**
1. Line 295: Change `client_secret: Option<String>` parameter type to `client_secret: Option<SecretString>`. Note: check the call site in main() to ensure the CLI-parsed or env var string is wrapped in `SecretString::from()` before being passed in.
2. Line 307: Wrap the env var fallback: `let client_secret = client_secret.or_else(|| std::env::var("OIDC_CLIENT_SECRET").ok().map(SecretString::from));`
3. Lines 324, 329: The clones (`secret_clone`, `client_secret_for_storage`) become `Option<SecretString>`. Where they are used in HTTP params (line 365-366), use `secret.expose_secret().as_str()`. Where stored in metadata JSON (line 537), use `.as_ref().map(|s| s.expose_secret().as_str())` or equivalent so the raw value goes to storage only.
4. Line 533: For refresh_token in metadata JSON: this is fine as-is since it comes from `token_result["refresh_token"].as_str()` (IdP response extraction for immediate storage write — the value is transient and goes directly to JSON, same as the access_token extraction before wrapping). No change needed here.

**main.rs — run_refresh() function (~line 585):**
1. Line 597-599: Wrap refresh_token at extraction: `let refresh_token = SecretString::from(metadata["refresh_token"].as_str().ok_or_else(|| anyhow::anyhow!("No refresh token found. Please login again."))?.to_string());`
2. Line 612: Wrap client_secret at extraction: `let client_secret: Option<SecretString> = metadata["client_secret"].as_str().map(|s| SecretString::from(s.to_string()));`
3. Line 617: The clone into spawn_blocking: `let refresh_token_clone = refresh_token;` (SecretString is Clone via Arc internally in secrecy 0.10). Alternatively, since it's moved into the closure, just move it directly.
4. Lines 628, 632-633: In the HTTP params inside spawn_blocking, use `refresh_token_clone.expose_secret().as_str()` and `secret.expose_secret().as_str()`.
5. Lines 677-679: The `new_refresh_token` extraction from response — this is for metadata storage. Keep as string for JSON serialization (same transient pattern as login path).
6. Line 691: `client_secret` in metadata JSON — use `.as_ref().map(|s| s.expose_secret().as_str())` to expose for storage write only.

**socket.rs — perform_token_refresh() function (~line 405):**
1. Lines 415-418: Wrap refresh_token: `let refresh_token = SecretString::from(metadata["refresh_token"].as_str().ok_or("No refresh token found. Please login again.")?.to_string());`
2. Line 431: Wrap client_secret: `let client_secret: Option<SecretString> = metadata["client_secret"].as_str().map(|s| SecretString::from(s.to_string()));`
3. Lines 444, 448-449: In HTTP params, use `refresh_token.expose_secret().as_str()` and `secret.expose_secret().as_str()`.
4. Lines 506, 510: In the updated_metadata JSON, `new_refresh_token` is already a plain &str from response extraction (transient for storage). `client_secret` in metadata: use `.as_ref().map(|s| s.expose_secret().as_str())`.

**Important:** Check that the call site of `run_login()` in main() wraps the CLI-parsed client_secret string in `SecretString::from()` if the parameter type changes. If the function signature change is too invasive (touches CLI parsing), keep the parameter as `Option<String>` and wrap immediately inside the function body instead (line 307 area).

Add MEM-03 security comments at each wrapping point, matching the style: `// Security (MEM-03): wrap in SecretString — must not appear in logs.`
  </action>
  <verify>
    <automated>cd /Users/cbc/code/apps/unix-oidc && cargo test -p unix-oidc-agent 2>&1 | tail -20</automated>
  </verify>
  <done>refresh_token and client_secret are SecretString in all four code locations (run_login, run_refresh in main.rs; perform_token_refresh in socket.rs). expose_secret() is the only path to raw values. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability table for MEM-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In `.planning/REQUIREMENTS.md`, update the traceability table (around line 77):
- Change `| MEM-03 | Phase 1 | Pending |` to `| MEM-03 | Phase 1 | Complete |`

This corrects the inconsistency noted in the VERIFICATION.md — the checklist at line 14 already shows `[x]` for MEM-03, but the traceability table still shows "Pending".
  </action>
  <verify>
    <automated>grep "MEM-03" /Users/cbc/code/apps/unix-oidc/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md traceability table shows MEM-03 as Complete, consistent with the checklist entry.</done>
</task>

</tasks>

<verification>
1. `cargo test -p unix-oidc-agent` passes — no regressions from type changes
2. `cargo clippy -p unix-oidc-agent -- -D warnings` passes — no new warnings
3. `grep -n "refresh_token.*String\b" unix-oidc-agent/src/main.rs unix-oidc-agent/src/daemon/socket.rs` shows no raw String usage for refresh_token (only SecretString)
4. `grep -n "client_secret.*String\b" unix-oidc-agent/src/main.rs unix-oidc-agent/src/daemon/socket.rs` shows no raw String usage for client_secret (only SecretString)
5. `grep -n "expose_secret" unix-oidc-agent/src/main.rs unix-oidc-agent/src/daemon/socket.rs` shows expose_secret() at HTTP param and storage write boundaries only
</verification>

<success_criteria>
- All three OAuth credential types (access_token, refresh_token, client_secret) are wrapped in SecretString
- No raw String variables hold token/secret values in main.rs or socket.rs
- expose_secret() is used only at HTTP form param construction and storage write points
- All existing tests pass
- REQUIREMENTS.md traceability is consistent
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-protection-hardening/01-04-SUMMARY.md`
</output>
